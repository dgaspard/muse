# Archive

This directory contains archived or experimental services and code that are not actively used or maintained.

## Contents

- `services/pipeline/` — Python FastAPI pipeline service (archived Jan 2026). Can be reactivated if pipeline automation is needed beyond orchestration via MusePipelineOrchestrator.

## Notes

- Archive items are kept for reference and potential future re-integration.
- Build artifacts, temporary test files, and backups are not stored here; see `.gitignore` for excluded patterns.
